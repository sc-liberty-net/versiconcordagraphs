# NRSV text — copyrighted, must never leave this machine
verses.js
verses.json
chapters.json
nrsv.txt
*.pdf
!findings.pdf

# STEPBible TAHOT/TAGNT source data — CC BY 4.0, but 100 MB, and STEPBible
# ask that it be fetched from github.com/STEPBible rather than redistributed.
# Re-fetch with fetch_lemmas.py; never commit.
lemma-data/

# Derived from the above by build_lemmas.py in a few seconds. No scripture
# text in it, but it is STEPBible's data reshaped, so it is not ours to
# redistribute either.
strongs_index.json

# scratch
__pycache__/
.DS_Store
desktop.ini

# Public-domain translation bulk downloads (fetch_translations.py). ~90 MB,
# and regenerable, so not committed. translations.json is the small extract.
translations-data/
